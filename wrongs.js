// Создай файл wrongs.js. В нем создай пять разных переменных с недопустимыми названиями.
// Выводить в консоль ничего не нужно.

let 1user
// не может начинаться с цифры
let my-name
// дефис '-' не разрешён в имени
let 10
// переменная не может быть числом
let var
// Имена переменных не должны совпадать с названием ключевых слов
let ^name
// кроме % и _ нельзя использовать символы